<section>
    <h1 class="text-4xl text-green-600 text-center">Product Details</h1>
    <div class="grid md:grid-cols-[20%_minmax(0,1fr)] items-center">
        <div>
            <img [src]="productDetails().imageCover" class="w-full object-contain" [alt]="productDetails().title" />
        </div>
        <div>
            <h2>{{productDetails().title}}</h2>
            <p class="text-gray-700 my-2">{{productDetails().description}}</p>
            <h3 class="text-green-600">{{productDetails().category.slug}}</h3>
            <div class="flex justify-between items-center">
                <span>{{productDetails().price}} EGP</span>
                <div class="flex items-center">
                    <i class="fas fa-star text-yellow-300"></i>
                    <span class="text-gray-500">
                        {{productDetails().ratingsAverage}}
                    </span>
                </div>
            </div>

            <button class="py-2 px-4 text-white rounded-lg w-[90%] block mx-auto bg-green-700 cursor-pointer">Add to
                cart</button>
        </div>
    </div>
</section>